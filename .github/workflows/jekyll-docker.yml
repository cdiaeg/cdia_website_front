name: Deploy to Hostinger

on:
  push:
    branches:
      - main 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Build React project
        run: npm run build

      - name: Upload to Hostinger
        uses: SamKirkland/FTP-Deploy-Action@3.1.1
        with:
          server: ftp://laseria.net
          username: u618083670.cdiaeg.com
          password: dgdf#@dsjfHAJS2
          server-dir: /
          local-dir: build
